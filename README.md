# Word-Frequency-Counter

• Built a Bash tool that reads a text file and outputs word/line frequency counts sorted by occurrence, using sort, uniq, and awk
• Added input validation to handle missing files and directory paths passed by mistake, with appropriate exit codes
